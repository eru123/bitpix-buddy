# BitPix Buddy

Lightweight desktop companion rendered with Odin, split into shared logic and native `linux` / `windows` entrypoints.

## Layout
- `common/` — shared pixel helpers
- `linux/` — Linux binary entrypoint
- `windows/` — Windows binary entrypoint
- `build/` — produced native binaries

## Build
```
bash build.sh
```
